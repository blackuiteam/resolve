import React, { useState } from 'react';

import { ClipTl, GeneratorTl, TextTl, TransitionTl } from './styles';

function Clips() {
	return (
		<>
			<ClipTl/>
			<GeneratorTl/>
			<TextTl/>
			<TransitionTl/>
		</>
	)
}

export default Clips;
